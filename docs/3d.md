# 3D Rendering

> **Owner subagent:** `r3f-engineer`
> **Touches:** Anything inside `<Canvas>`, Three.js, React Three Fiber, drei, `useGLTF`, scene rendering, `WorldViewer` and related
> **Does NOT touch:** API routes, page chrome (header/footer/forms), DB

## Stack

- **Renderer:** React Three Fiber (R3F)
- **Helpers:** `@react-three/drei` (Bounds, Center, useGLTF, OrbitControls, etc.)
- **Engine:** Three.js (underlying)
- **Asset format:** `.glb` (binary glTF 2.0) — currently the entire world is one file
- **Phase 2 target:** scene graph composition — many `.glb` assets referenced from a JSON scene graph

## Current World Rendering

A world today = one `.glb` file. The viewer:

1. Loads the `.glb` via `useGLTF` (drei)
2. Wraps in `<Bounds>` (drei) to auto-fit the camera to the model's bounding box
3. Wraps in `<Center>` (drei) to recenter at origin
4. Renders inside a `<Canvas>` with OrbitControls + lighting + fullscreen toggle

### `<WorldViewer>` component

- File: `src/components/world-viewer/WorldViewer.tsx`
- Props: `glbUrl: string` (required), `ariaLabel?: string` (optional, defaults to `"3D world viewer"`)
- Lazy-loaded on `/world/[id]` page via `next/dynamic` with `ssr: false` in `src/app/world/[id]/WorldViewerClient.tsx`. Not imported in the feed — Three.js stays out of that bundle.
- Fullscreen toggle: a `<FullscreenButton>` component (defined in the same file) holds a `containerRef` to the wrapping `<div>`. On click it calls `containerRef.current.requestFullscreen()` to enter or `document.exitFullscreen()` to exit. It syncs button state by listening to the `fullscreenchange` DOM event. Errors (e.g., iframe restrictions) are caught and logged as a warning; the UI does not crash.

## Asset Loading Patterns

- `useGLTF` (drei) for the primary world model. The `<Model url={glbUrl}>` inner component calls `useGLTF` and suspends until the file is ready; it is wrapped in `<Suspense fallback={null}>` inside the Canvas.
- `useGLTF.preload(url)` is not called in the current implementation. It is documented as a future optimisation (trigger from the feed when navigation to a world is imminent).
- Error handling: `<WorldViewerErrorBoundary>` (class component — `src/components/world-viewer/WorldViewerErrorBoundary.tsx`) wraps the whole viewer. Any render error, including a failed GLB load, is caught and replaced with `<ViewerError>`, which shows "Couldn't load this world — The 3D file may be corrupted or unreachable. Try refreshing."
- Loading state UI: `<LoadingOverlay>` reads `useProgress().active` from drei (a zustand hook, callable outside the Canvas) and renders `<ViewerLoading>` — a semi-transparent backdrop with "Loading world…" text — while a load is in progress. Both fallback components live in `src/components/world-viewer/WorldViewerFallback.tsx`.

## Performance Budget

- **`.glb` size cap:** 50 MB (enforced in `POST /api/uploads/sign` via `KIND_RULES.glb.maxBytes = 50 * 1024 * 1024`). The 25 MB figure in early planning was not what shipped.
- **Polygon count:** no hard cap enforced. No specific threshold has been measured yet for lower-end devices; this is a future optimisation concern.
- **Texture size:** no explicit cap enforced on texture dimensions or total texture memory. The 50 MB GLB cap is the only hard constraint on the asset today.
- **Feed-card preview:** `preload="none"` on `<video>` (handled by `WorldCardMedia`, not 3D-rendered in the feed)

## Lighting Defaults

- Ambient light: `<ambientLight intensity={0.5} />` — no explicit color (defaults to white `#ffffff`)
- Directional light: `<directionalLight position={[5, 5, 5]} intensity={1} />` — no explicit color (white), no shadow casting configured
- HDRI environment: `<Environment preset="studio" />` from drei is used, loaded inside the `<Suspense>` block. It provides image-based lighting (IBL) on top of the two explicit lights. There is no hemisphere light in the current implementation.

## Camera Defaults

- Initial position: `[3, 3, 5]` (passed to `<Canvas camera={{ position: [3, 3, 5], fov: 50 }}>`)
- FOV: `50` degrees
- `<Bounds fit clip observe margin={1.4}>` overrides the initial position at mount to auto-fit the loaded model's bounding box. `margin={1.4}` adds 40% breathing room so the model is not flush against the frustum edges.
- OrbitControls: `enableDamping`, `dampingFactor={0.05}`, `minDistance={0.5}`, `maxDistance={50}`. No polar angle clamps are set (full vertical orbit allowed).

## Phase 2 — Scene Graph Rendering (Active)

Phase 2 transforms a world from "one .glb file" to "a scene graph that composes many `.glb` assets in 3D space." The renderer split (sub-slice 8.1) is now shipped.

### Scene graph schema (v1)

The TypeScript schema lives in `src/lib/scene-graph/schema.ts`. Key shapes:

- `ObjectSchema` — `{ id, assetId (uuid FK to world_assets), name?, position, rotation (Euler radians), scale }`. Rotations are Euler vec3 in v1 for editor simplicity; planned switch to quaternion in v2 when an animation system needs the precision.
- `LightSchema` — discriminated union on `type`: `"ambient"` (`intensity`, `color`) or `"sun"` (`intensity`, `direction`, `color`). No point/spot lights in v1.
- `EnvironmentSchema` — `skybox` enum (`"studio" | "sunset" | "dawn" | "night" | "warehouse" | "park" | "city" | "forest"`) + nullable `fog` (`color`, `near`, `far`).
- `CameraSchema` — `position`, `target`, `fov`. In the renderer, `target` is intentionally not applied — `<Bounds>` takes over at mount. Revisit in v2 if authored target should override Bounds.
- Top-level `SceneGraphV1` — `{ schemaVersion: 1, objects[], lights[], environment, spawnPoints[], camera }`.

Parse with `parseSceneGraph(unknown): SceneGraphAny` — throws on invalid input; caller decides whether to fall through to legacy. Build an empty graph with `emptySceneGraph(): SceneGraphV1`.

### Renderer branching

- If `worlds.scene_graph` is present and parseable → `<SceneGraphRendererClient>` → `SceneGraphRenderer`
- Else (null or parse failure) → `<WorldViewerClient>` → legacy `WorldViewer`
- Branch point: `src/app/world/[id]/page.tsx` (task 8 / frontend-dev)

### `<SceneGraphRenderer>` — how it works

**Component structure:**

- `SceneGraphRenderer` (exported default) — outer shell: `WorldViewerErrorBoundary`, container div, `FullscreenButton`, `Canvas`, `LoadingOverlay`. Camera position + fov taken from `sceneGraph.camera`.
- `SceneGraphScene` — inner component inside the Suspense boundary: renders lights (discriminated union), `<Environment>`, `<fog>`, and one `<AssetObject>` per `sceneGraph.objects` entry.
- `AssetObject` — calls `useGLTF(assetUrl)` and returns `<group position rotation scale><primitive object={cloned} /></group>`. The `cloned` is `useMemo(() => scene.clone(true), [scene])`.
- `LoadingOverlay` — reads `useProgress().active` outside the Canvas (same pattern as `WorldViewer`).
- `FullscreenButton` — identical logic to the inlined version in `WorldViewer`.

**`scene.clone(true)` per instance:** drei caches `useGLTF` results by URL — all consumers of the same URL share one `THREE.Object3D` tree. Without cloning, two objects at different positions pointing at the same asset would fight over a single transform. `scene.clone(true)` gives each `<AssetObject>` its own independent copy of the tree. The binary is not re-fetched — the URL cache is still hit. Note: `scene.clone(true)` does NOT correctly deep-clone `SkinnedMesh` skeletons. Phase 2 v1 does not support skinned animations, so plain clone suffices. Switch to `SkeletonUtils.clone` if/when animation support arrives.

**Missing assets (non-fatal skip):** if `obj.assetId` is not found in the `assets[]` prop, a `console.warn` is emitted and that object is skipped. The rest of the scene renders normally. Load failures are never fatal.

**Lights rendering pattern:**
```tsx
{sceneGraph.lights.map((light, i) => {
  if (light.type === "ambient") {
    return <ambientLight key={i} intensity={light.intensity} color={light.color} />;
  }
  if (light.type === "sun") {
    return <directionalLight key={i} intensity={light.intensity} position={light.direction} color={light.color} />;
  }
  return null; // future light types
})}
```

**Environment + fog:**
```tsx
{sceneGraph.environment.skybox && <Environment preset={sceneGraph.environment.skybox} />}
{sceneGraph.environment.fog && (
  <fog attach="fog" args={[fog.color, fog.near, fog.far]} />
)}
```

**Spawn points** are not visualized in the viewer. They are stored in the scene graph for Phase 3 avatar entry only.

**Lazy-load pattern:** `SceneGraphRendererClient` uses `next/dynamic({ ssr: false })` to keep Three.js out of server bundles — same pattern as `WorldViewerClient`.

### Performance considerations

- **Instancing** — same `assetId` repeated many times should share GPU geometry (`<Instances>` from drei). Not implemented in v1; add during 8.4 stress-test if needed.
- **Frustum culling** — Three.js does this by default; ensure it's not disabled.
- **LOD** — not in v1; asset model designed to allow LOD variants later.
- **Async loading** — partial scene renders as individual GLB assets stream in (each `<AssetObject>` suspends independently within the shared `<Suspense>` boundary).

### Editor mode (Phase 2, sub-slice 8.4)

The in-browser editor at `/world/[id]/edit` will wrap `SceneGraphRenderer`'s canvas and add:

- Transform gizmos (`<TransformControls>` from drei)
- Selection state (clicked object highlighted)
- Drag-to-place from asset panel
- Zustand store for editor-only state
- Touch-friendly gizmos (tablets day one — touch handling is a real research item)

## Phase 3+ Prep

- **Shared presence:** capsule avatars rendered alongside the world. Simple — sphere + cylinder + name label sprite.
- **Realtime sync:** position/rotation updates from collaborators arrive via Liveblocks (or Yjs) and update avatar transforms.
- **Realtime editing:** scene graph mutations from other editors apply in real time. R3F's reactive rendering handles this for free if state is shared correctly.

## Gotchas

- **`useGLTF` caches by URL** — if you re-upload to the same URL, the cached version may persist. Force a refresh by appending a versioned query param (`?v=2`).
- **`<Suspense>` is required** around `useGLTF` consumers in R3F — provide a fallback or the canvas crashes on first load.
- **Pointer events on `<Canvas>`** — can interfere with page scrolling. Make sure the canvas doesn't capture wheel events when not focused.
- **R3F + Next.js App Router** — `<Canvas>` MUST be in a client component (`"use client"` directive at the top). Server-rendering R3F crashes.

## Slice 7 (3D-related)

Slice 7 has no 3D changes. View counts are tracked on the page, not in the 3D layer.

## Files

| Path | Purpose |
|---|---|
| `src/components/world-viewer/WorldViewer.tsx` | Legacy viewer component. Exports `default WorldViewer`. Contains `Model`, `LoadingOverlay`, and `FullscreenButton` sub-components. Renders `<Canvas>`, lighting, `<Bounds>`, `<Center>`, `<OrbitControls>`, and `<Environment>`. Used for all worlds where `scene_graph` is NULL. |
| `src/components/world-viewer/WorldViewerErrorBoundary.tsx` | Class-based React error boundary. Catches render errors (including GLB load failures) and renders the `fallback` prop instead. Reused by `SceneGraphRenderer`. Props: `{ children, fallback: ReactNode }`. |
| `src/components/world-viewer/WorldViewerFallback.tsx` | Two named exports: `ViewerLoading` (progress overlay shown while THREE.DefaultLoadingManager is active) and `ViewerError` (shown by the error boundary on failure). Reused by `SceneGraphRenderer`. |
| `src/app/world/[id]/WorldViewerClient.tsx` | Thin client wrapper that lazy-loads `WorldViewer` via `next/dynamic({ ssr: false })`, keeping Three.js out of the feed page bundle. |
| `src/components/scene-graph-renderer/SceneGraphRenderer.tsx` | Scene-graph renderer (Phase 2). Accepts `{ sceneGraph: SceneGraphV1, assets: Asset[], ariaLabel? }`. Contains `SceneGraphScene`, `AssetObject`, `LoadingOverlay`, and `FullscreenButton` sub-components. Reads lights/environment/fog from the scene graph; renders one `<AssetObject>` per `sceneGraph.objects` entry; uses `scene.clone(true)` per instance. Reuses `WorldViewerErrorBoundary` and `ViewerLoading`/`ViewerError` from the world-viewer package. |
| `src/components/scene-graph-renderer/SceneGraphRendererClient.tsx` | Thin client wrapper that lazy-loads `SceneGraphRenderer` via `next/dynamic({ ssr: false })`, keeping Three.js out of server bundles. Mirrors `WorldViewerClient`. Named export `SceneGraphRendererClient`. |
| `src/lib/scene-graph/schema.ts` | `SceneGraphV1` Zod schema + TypeScript types + `parseSceneGraph()` + `emptySceneGraph()` helpers. The authoritative definition of what a scene graph looks like. |